services:
  web:
    build: .
    container_name: greenfield-web
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      DB_DRIVER: ${DB_DRIVER}
      DATABASE_URL: ${DATABASE_URL}
